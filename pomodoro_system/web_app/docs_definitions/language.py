language_header_definition = {
    "Accept-Language": {
        "in": "header",
        "type": "string",
        "required": False,
        "description": "Language code.",
        "enum": ["en", "pl"],
        "default": "en",
    }
}
